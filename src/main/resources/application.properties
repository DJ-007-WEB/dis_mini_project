spring.application.name=course_management_system

# Database settings
# These are the default values. For production, set environment variables:
# DB_URL, DB_USER, DB_PASSWORD
db.url=jdbc:mysql://localhost:3306/course_management_db
db.user=cms_user
db.password=CmsPass123!
db.pool.size=10
